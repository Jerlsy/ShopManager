<UserControl x:Class="ShopManager.Views.Dialogs.ScheduleRulesDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="460">
    <Border Background="{DynamicResource AppSurfaceBrush}"
            BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="1"
            CornerRadius="20" Padding="28,24,28,16">
        <StackPanel>
            <TextBlock Text="排班規則"
                       FontSize="{DynamicResource FontSizeDialogTitle}" FontWeight="SemiBold"
                       Margin="0,0,0,20"/>

            <!-- 固定休假 -->
            <TextBlock Text="固定休假" Style="{StaticResource FieldLabelStyle}"/>
            <ItemsControl x:Name="FixedOffList" Margin="0,4,0,20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Label}"
                                  IsChecked="{Binding IsChecked}"
                                  IsEnabled="{Binding IsUserEditable}"
                                  Margin="0,0,16,6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 排除班別 -->
            <TextBlock Text="排除班別" Style="{StaticResource FieldLabelStyle}"/>
            <TextBlock x:Name="NoShiftsHint"
                       Text="（目前無啟用中的班別）"
                       Style="{StaticResource CaptionTextStyle}"
                       Margin="0,4,0,20" Visibility="Collapsed"/>
            <ItemsControl x:Name="ExcludeShiftList" Margin="0,4,0,20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Alias}" IsChecked="{Binding IsChecked}"
                                  Margin="0,0,16,6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 不與共事 -->
            <TextBlock Text="不與共事" Style="{StaticResource FieldLabelStyle}"/>
            <TextBlock x:Name="NoColleaguesHint"
                       Text="（目前無其他在職員工）"
                       Style="{StaticResource CaptionTextStyle}"
                       Margin="0,4,0,20" Visibility="Collapsed"/>
            <ItemsControl x:Name="NotWithList" Margin="0,4,0,20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"
                                  Margin="0,0,16,6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                <Button Content="取消"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{x:Static md:DialogHost.CloseDialogCommand}"
                        CommandParameter="{x:Null}"
                        Margin="0,0,8,0"/>
                <Button Content="確認"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="{DynamicResource PrimaryHueMidBrush}"
                        Click="OkButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
